import userSlice from './user/userSlice';
import counterSlice from './counter/counterSlice';
import itemSlice from './item/itemSlice';
import pokeSlice from './pokemon/pokeSlice';

export default {
  counter: counterSlice,
  user: userSlice,
  item: itemSlice,
  pokemon: pokeSlice,
};
